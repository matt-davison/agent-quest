# Git Configuration
# These will be used for commits inside the container
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=your.email@example.com
GIT_COMMITTER_NAME=Your Name
GIT_COMMITTER_EMAIL=your.email@example.com

# Note: GitHub PAT and Claude API keys are configured via setup.sh
# They are stored in ./data/.config/ (gitignored, persistent)
